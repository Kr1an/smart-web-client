// import React from 'react';
// import { shallow } from 'enzyme';

// import PagesNavigatorElement from '../index';

describe('<PagesNavigatorElement />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
