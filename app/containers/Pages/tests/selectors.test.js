// import { fromJS } from 'immutable';
// import { makeSelectPagesDomain } from '../selectors';

// const selector = makeSelectPagesDomain();

describe('makeSelectPagesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
