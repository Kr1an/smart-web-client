// import { fromJS } from 'immutable';
// import { makeSelectStructureDomain } from '../selectors';

// const selector = makeSelectStructureDomain();

describe('makeSelectStructureDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
