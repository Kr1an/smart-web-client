// import { fromJS } from 'immutable';
// import { makeSelectPagesNavigatorDomain } from '../selectors';

// const selector = makeSelectPagesNavigatorDomain();

describe('makeSelectPagesNavigatorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
