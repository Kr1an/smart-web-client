// import { fromJS } from 'immutable';
// import { makeSelectPageDomain } from '../selectors';

// const selector = makeSelectPageDomain();

describe('makeSelectPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
